FROM mysql:5.7

# Install MySQL client tools, including mysqladmin

# Set environment variables for MySQL
ENV MYSQL_ROOT_PASSWORD=nandhu01
ENV MYSQL_DATABASE=resume_analyzer

# Expose MySQL port
EXPOSE 3306

# Default command (MySQL will run automatically)
CMD ["mysqld"]
